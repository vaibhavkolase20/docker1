# docker basic command

  1) docker ps == to show conatainer list
  2) docker pull == to pull images trought docker hub
  3) docker images == to show list of images

# this is the commnd for run the mysql image
  1) docker run -d -e MYSQL_ROOT_PASSWORD=root mysql
  docker exec -it e3d3ca194b64 bash == to access this mysql container
  mysql -u root -p == to run mysqul
 CREATE DATABASE my_new_db; == to create database
USE <database_name>;=== to acess created database

create table  using this information
CREATE TABLE my_table (
  id INT AUTO_INCREMENT PRIMARY KEY,
  name VARCHAR(100),
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);



  



# if you want to stop your container then you use
2) docker stop container id
